[project]
name = "archcouncil"
version = "0.1.0"
description = "Multi-agent architecture review system built on Band"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "band-sdk[anthropic]>=0.2.11",
    "python-dotenv>=1.2.2",
    "streamlit>=1.58.0",
]
